# 样例对照

## Intent：观察段落感

这些是固定测试集中选取的简短样例，包含模型的改善与不足，不代表整体质量。原作者参考仅用于比较布局，不能替代用户审美判断。

## Data：真实来源与模型输出

### 01-001 · typescript

来源：`colinhacks/zod`，文件 `data/raw/colinhacks/zod/62311eb2c4a93c7348a8b1c5abbde1ec77c66b67/packages/zod/src/v4/core/api.ts`。

输入：

````typescript
  });
}
// @__NO_SIDE_EFFECTS__
export function _coercedDate<T extends schemas.$ZodDate>(
  Class: util.SchemaClass<T>,
  params?: string | $ZodDateParams
): T {
  return new Class({
    type: "date",
    coerce: true,
    ...util.normalizeParams(params),
  });
}
// NaN
export type $ZodNaNParams = TypeParams<schemas.$ZodNaN>;

````

模型输出：

````typescript
  });
}

// @__NO_SIDE_EFFECTS__
export function _coercedDate<T extends schemas.$ZodDate>(
  Class: util.SchemaClass<T>,
  params?: string | $ZodDateParams
): T {
  return new Class({
    type: "date",
    coerce: true,
    ...util.normalizeParams(params),
  });
}

// NaN
export type $ZodNaNParams = TypeParams<schemas.$ZodNaN>;

````

原作者参考：

````typescript
  });
}
// @__NO_SIDE_EFFECTS__
export function _coercedDate<T extends schemas.$ZodDate>(
  Class: util.SchemaClass<T>,
  params?: string | $ZodDateParams
): T {
  return new Class({
    type: "date",
    coerce: true,
    ...util.normalizeParams(params),
  });
}

// NaN
export type $ZodNaNParams = TypeParams<schemas.$ZodNaN>;

````

### 03-001 · python

来源：`encode/httpx`，文件 `data/raw/encode/httpx/b5addb64f0161ff6bfe94c124ef76f6a1fba5254/httpx/_client.py`。

输入：

````python
        Cookie values to include when sending requests.
        """
        return self._cookies
    @cookies.setter
    def cookies(self, cookies: CookieTypes) -> None:
        self._cookies = Cookies(cookies)
    @property
    def params(self) -> QueryParams:
        """
        Query parameters to include in the URL when sending requests.
        """
        return self._params
    @params.setter
    def params(self, params: QueryParamTypes) -> None:
        self._params = QueryParams(params)
    def build_request(
        self,
        method: str,
        url: URL | str,

````

模型输出：

````python
        Cookie values to include when sending requests.
        """
        return self._cookies
    @cookies.setter
    def cookies(self, cookies: CookieTypes) -> None:
        self._cookies = Cookies(cookies)
    @property
    def params(self) -> QueryParams:
        """
        Query parameters to include in the URL when sending requests.
        """
        return self._params
    @params.setter
    def params(self, params: QueryParamTypes) -> None:
        self._params = QueryParams(params)
    def build_request(
        self,
        method: str,
        url: URL | str,

````

原作者参考：

````python
        Cookie values to include when sending requests.
        """
        return self._cookies

    @cookies.setter
    def cookies(self, cookies: CookieTypes) -> None:
        self._cookies = Cookies(cookies)

    @property
    def params(self) -> QueryParams:
        """
        Query parameters to include in the URL when sending requests.
        """
        return self._params

    @params.setter
    def params(self, params: QueryParamTypes) -> None:
        self._params = QueryParams(params)

    def build_request(
        self,
        method: str,
        url: URL | str,

````

### 07-001 · go

来源：`gin-gonic/gin`，文件 `data/raw/gin-gonic/gin/dcaa4296d111981ffb31ac3eba90bb63e1eb5ab9/render/html.go`。

输入：

````go
func (r HTML) Render(w http.ResponseWriter) error {
	r.WriteContentType(w)
	if r.Name == "" {
		return r.Template.Execute(w, r.Data)
	}
	return r.Template.ExecuteTemplate(w, r.Name, r.Data)
}
// WriteContentType (HTML) writes HTML ContentType.
func (r HTML) WriteContentType(w http.ResponseWriter) {
	writeContentType(w, htmlContentType)
}

````

模型输出：

````go
func (r HTML) Render(w http.ResponseWriter) error {
	r.WriteContentType(w)
	if r.Name == "" {
		return r.Template.Execute(w, r.Data)
	}

	return r.Template.ExecuteTemplate(w, r.Name, r.Data)
}

// WriteContentType (HTML) writes HTML ContentType.
func (r HTML) WriteContentType(w http.ResponseWriter) {
	writeContentType(w, htmlContentType)
}

````

原作者参考：

````go
func (r HTML) Render(w http.ResponseWriter) error {
	r.WriteContentType(w)

	if r.Name == "" {
		return r.Template.Execute(w, r.Data)
	}
	return r.Template.ExecuteTemplate(w, r.Name, r.Data)
}

// WriteContentType (HTML) writes HTML ContentType.
func (r HTML) WriteContentType(w http.ResponseWriter) {
	writeContentType(w, htmlContentType)
}

````

### 09-001 · csharp

来源：`DapperLib/Dapper`，文件 `data/raw/DapperLib/Dapper/6d48ef664acc7298c649e2d449d903b3360d5a90/Dapper/SqlMapper.DapperRowMetaObject.cs`。

输入：

````csharp
﻿using System;
using System.Collections.Generic;
using System.Reflection;
namespace Dapper
{
    public static partial class SqlMapper
    {
        private sealed partial class DapperRow : System.Dynamic.IDynamicMetaObjectProvider
        {
            System.Dynamic.DynamicMetaObject System.Dynamic.IDynamicMetaObjectProvider.GetMetaObject(
    System.Linq.Expressions.Expression parameter)
            {

````

模型输出：

````csharp
﻿using System;

using System.Collections.Generic;
using System.Reflection;

namespace Dapper
{
    public static partial class SqlMapper
    {
        private sealed partial class DapperRow : System.Dynamic.IDynamicMetaObjectProvider
        {
            System.Dynamic.DynamicMetaObject System.Dynamic.IDynamicMetaObjectProvider.GetMetaObject(

    System.Linq.Expressions.Expression parameter)
            {

````

原作者参考：

````csharp
﻿using System;
using System.Collections.Generic;
using System.Reflection;
namespace Dapper
{
    public static partial class SqlMapper
    {
        private sealed partial class DapperRow : System.Dynamic.IDynamicMetaObjectProvider
        {
            System.Dynamic.DynamicMetaObject System.Dynamic.IDynamicMetaObjectProvider.GetMetaObject(
    System.Linq.Expressions.Expression parameter)
            {

````

### 10-001 · ruby

来源：`sinatra/sinatra`，文件 `data/raw/sinatra/sinatra/cb22afd7902b566b6eaba6c4ea89739494a65d12/lib/sinatra/base.rb`。

输入：

````ruby
        force_encoding data, encoding
        data.each_line do |line|
          lines += 1
          if line =~ /^@@\s*(.*\S)\s*$/
            template = force_encoding(String.new, encoding)
            templates[$1.to_sym] = [template, file, lines]
          elsif template
            template << line
          end
        end
      end
      # Lookup or register a mime type in Rack's mime registry.
      def mime_type(type, value = nil)
        return type      if type.nil?
        return type.to_s if type.to_s.include?('/')

````

模型输出：

````ruby
        force_encoding data, encoding
        data.each_line do |line|
          lines += 1
          if line =~ /^@@\s*(.*\S)\s*$/
            template = force_encoding(String.new, encoding)
            templates[$1.to_sym] = [template, file, lines]
          elsif template
            template << line
          end
        end
      end
      # Lookup or register a mime type in Rack's mime registry.
      def mime_type(type, value = nil)
        return type      if type.nil?
        return type.to_s if type.to_s.include?('/')

````

原作者参考：

````ruby
        force_encoding data, encoding
        data.each_line do |line|
          lines += 1
          if line =~ /^@@\s*(.*\S)\s*$/
            template = force_encoding(String.new, encoding)
            templates[$1.to_sym] = [template, file, lines]
          elsif template
            template << line
          end
        end
      end

      # Lookup or register a mime type in Rack's mime registry.
      def mime_type(type, value = nil)
        return type      if type.nil?
        return type.to_s if type.to_s.include?('/')

````

## Edges：边界

代码片段保留评分所用上下文，但此处为阅读而统一显示 LF。部分陌生语法或异常缩进会导致不理想的留白，尤其应检查 C# 参数列表等位置。完整 600 个案例见 data/evaluation，各项指标见训练与测试结果文档。

## Answer：审美检查标准

检查相关语句是否成组、转折是否留白、参数与字段是否被无意义拆开，以及短语句是否形成合适节奏；不要只看空行数量。
