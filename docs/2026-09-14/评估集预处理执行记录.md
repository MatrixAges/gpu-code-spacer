# 评估集预处理执行记录

## Intent：最终目标

完成 evaluation 中 90 个共享期望输出的人工留白预处理，依据 `rules.md` 比较同层级相邻完整语句的整体书写形态。本文件与《评估集预处理计划》共同采用 IDEA 框架。

## Data：可用证据与结果

| 范围 | 已阅读 | 调整 | 保留 | 增加空行 | 删除空行 |
| --- | ---: | ---: | ---: | ---: | ---: |
| cases | 60 | 5 | 55 | 5 | 0 |
| validation | 30 | 13 | 17 | 21 | 0 |
| 合计 | 90 | 18 | 72 | 26 | 0 |

原始 1,621 行已全部阅读，直接应用人工确定的空行补丁，没有使用自动推断留白的脚本。执行前快照、前后哈希、完整差异与逐文件核查数据保存在 `artifacts/evaluation-preprocess-20260914/`，其中 `verification.json` 记录交付 SHA-256。

## Edges：边界与限制

- 90 个 output 的非空行字节序列（包括缩进、顺序和原有行尾）与本轮开始时一致。
- 全部 180 个 dense/spaced 输入与对应 output 的非空行字节序列一致。
- evaluation 文件清单不变，全部非目标文件哈希不变，包括输入、元数据、说明和历史产物。
- 未修改 dataset 或产品代码，未运行训练、模型评分、格式化器、测试或浏览器。
- 元数据中的 initial_output_sha256 和原始 annotation 仍是历史生成记录，不改写为本轮结果；本轮记录以交付哈希及当前 output 为准。
- validation 仅做预标注校正，没有依据模型失败开发或选模，仍保留其独立验证用途。

## Answer：交付与自我审查

90 个输出均已完成本轮人工审阅，没有未处理文件；助手预处理结果整体仍待用户审美审核。

对全部 26 个补丁位置再次复核，新增空行均位于声明、调用、赋值等完整结构之间，没有拆开参数、字符串、注释正文或分支附属结构。审查特别保留了相近的单行调用组、数值声明组及重复索引运算组，未因函数名、接收者身份或语义职责变化而普遍拆段。

自我批判：非空行保真只能证明编辑范围，不能证明视觉判断必然正确。Dart 存储引用与坐标声明、Go 算术与切片声明、OCaml 复杂调用与关闭调用等位置，是依据完整表现形式作出的判断，不能推广成类型名、函数名或局部语法不同就必定分段的规则。此次没有用新增通用规则替代用户已有实例，后续应以用户具体校正继续校准。

### 逐文件记录

| 文件 | 结果 | 增加空行 | 判断记录 |
| --- | --- | ---: | --- |
| `evaluation/cases/cpp-bounds/output.cpp` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/cpp-reserve/output.cpp` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/cpp-score/output.cpp` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/cs-message/output.cs` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/cs-page/output.cs` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/cs-tags/output.cs` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/go-broadcast/output.go` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/go-capacity/output.go` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/go-vector/output.go` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-cpp-average/output.cpp` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-cpp-erase/output.cpp` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-cs-count/output.cs` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-cs-parse/output.cs` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-go-map/output.go` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-go-partition/output.go` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-java-search/output.java` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-java-write/output.java` | 调整 | 1 | 导入组与多行类声明分隔。 |
| `evaluation/cases/holdout-js-index/output.js` | 调整 | 1 | 成员调用 names.sort 与直接调用 report 分隔。 |
| `evaluation/cases/holdout-js-retry/output.js` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-kt-cache/output.kt` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-kt-join/output.kt` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-py-groups/output.py` | 调整 | 1 | min 调用结果赋值与切片引用赋值分隔。 |
| `evaluation/cases/holdout-py-names/output.py` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-rb-lookup/output.rb` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-rb-tally/output.rb` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-rust-fold/output.rs` | 调整 | 1 | 常量递增与成员调用结果累加分隔。 |
| `evaluation/cases/holdout-rust-key/output.rs` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-swift-save/output.swift` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-swift-total/output.swift` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-ts-buffer/output.ts` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-ts-route/output.ts` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-zig-search/output.zig` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/holdout-zig-slice/output.zig` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/java-console/output.java` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/java-pair/output.java` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/java-timeout/output.java` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/js-coordinate/output.js` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/js-limit/output.js` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/js-notify/output.js` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/kt-collect/output.kt` | 调整 | 1 | 相近成员调用保持连续，与直接 publish 调用分隔。 |
| `evaluation/cases/kt-lines/output.kt` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/kt-quota/output.kt` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/py-log/output.py` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/py-options/output.py` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/py-price/output.py` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/rb-age/output.rb` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/rb-emit/output.rb` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/rb-settings/output.rb` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/rust-queue/output.rs` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/rust-tail/output.rs` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/rust-window/output.rs` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/swift-duration/output.swift` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/swift-events/output.swift` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/swift-range/output.swift` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/ts-audit/output.ts` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/ts-label/output.ts` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/ts-stock/output.ts` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/zig-clamp/output.zig` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/zig-components/output.zig` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/cases/zig-copy/output.zig` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/dart-flutter-alignment-lerp/output.dart` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/dart-flutter-curve-inverse/output.dart` | 调整 | 1 | 简单数值声明组与带修饰、显式类型的调用初始化分隔。 |
| `evaluation/validation/dart-flutter-decode-envelope/output.dart` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/dart-flutter-transform-point/output.dart` | 调整 | 1 | 列表类型存储引用与 double 坐标声明组分隔；坐标引用与后续索引运算组之间原有注释前留白保留。 |
| `evaluation/validation/dart-flutter-velocity-estimate/output.dart` | 调整 | 1 | 整数索引运算声明与可空对象下标引用声明分隔，组内两句仍连续。 |
| `evaluation/validation/go-hugo-emojify/output.go` | 调整 | 3 | 数值初始化与调用初始化、算术声明与调用声明、算术声明与切片声明分隔。 |
| `evaluation/validation/go-hugo-open-file/output.go` | 调整 | 1 | 单值引用声明与双值调用声明分隔，空行置于后者附属注释之前。 |
| `evaluation/validation/go-hugo-permalink/output.go` | 调整 | 1 | 路径调用结果赋值与两个直接成员引用赋值分隔。 |
| `evaluation/validation/go-hugo-slice/output.go` | 调整 | 1 | 下标引用声明与反射调用声明分隔。 |
| `evaluation/validation/go-hugo-unwrap-hashing/output.go` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/lua-kong-deep-copy/output.lua` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/lua-kong-module-exports/output.lua` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/lua-kong-normalize-uri/output.lua` | 调整 | 3 | 直接调用声明与 and/or 条件声明组分隔；下标写入与简单变量调用赋值、字符串赋值分隔。 |
| `evaluation/validation/lua-kong-split/output.lua` | 调整 | 2 | 数值声明组与表构造调用声明分隔；下标调用结果写入与简单数值推进组分隔。 |
| `evaluation/validation/lua-kong-validate-address/output.lua` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/ocaml-infer-file-diff/output.ml` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/ocaml-infer-normalize-path/output.ml` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/ocaml-infer-process-output/output.ml` | 调整 | 2 | 管道组合绑定与直接格式化调用绑定分隔；带嵌套标签参数的调用与简单关闭调用组分隔。 |
| `evaluation/validation/ocaml-infer-source-file/output.ml` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/ocaml-infer-stats-event/output.ml` | 调整 | 1 | 局部模块打开与多行函数绑定分隔；管道与 match 分支内部保持。 |
| `evaluation/validation/php-laravel-call-class/output.php` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/php-laravel-collapse/output.php` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/php-laravel-file-replace/output.php` | 调整 | 1 | 带条件回退的赋值与嵌套调用赋值分隔。 |
| `evaluation/validation/php-laravel-rate-attempt/output.php` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/php-laravel-replace-array/output.php` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/scala-spark-change-value/output.scala` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/scala-spark-grow-buffer/output.scala` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/scala-spark-next-set-bit/output.scala` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
| `evaluation/validation/scala-spark-reservoir-sampling/output.scala` | 调整 | 3 | 数组构造与数值声明、下标赋值与复合递增、成员引用与对象构造声明分隔；注释保持贴附。 |
| `evaluation/validation/scala-spark-storage-flags/output.scala` | 保留 | 0 | 现有相邻语句分组、完整表达式外侧留白及注释贴附可保留。 |
